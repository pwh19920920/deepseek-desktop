{
  "name": "dsh-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "DeepSeek Harness Desktop - Tauri 2 app wrapping deepseek-harness",
  "license": "MIT",
  "scripts": {
    "dev": "cd apps/desktop && pnpm dev",
    "build": "node scripts/build.js",
    "clean": "node scripts/clean.js",
    "fetch-node": "node scripts/fetch-node.js",
    "check": "cd apps/desktop/src-tauri && cargo check && cargo clippy -- -D warnings && cargo fmt --check"
  }
}
