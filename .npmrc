# Ignore native build scripts that are not needed for the desktop app.
# The @deepseek-ai/dsh package bundles its own Node.js, so these native
# dependencies (node-pty, koffi, etc.) are not required at runtime.
ignore-scripts = true
